# FastHTML Toast Tester

Testing scenarios:

- On page load
- HTMX request
- Websocket
- Redirect
- RedirectReponse


## to create requirements.txt run
```bash
uv export --no-hashes --format requirements-txt > requirements.txt
```